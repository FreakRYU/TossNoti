# Placeholder for the Secrets Gradle Plugin. TossNoti does not currently
# consume any build-time secrets, but the plugin requires this file to
# exist (referenced from app/build.gradle.kts).
#
# Add KEY=value lines here if you ever introduce environment-injected
# build config fields.
